version: "2"

linters:
  settings:
    errcheck:
      # Terminal/report output: a failed write to stdout has no useful recovery.
      exclude-functions:
        - fmt.Fprintf
        - fmt.Fprintln
        - fmt.Fprint
